Install XAMPP.

Start apache and MySQL servers

go to MySQL admin (http://localhost/phpmyadmin)

create a new database called 'ecelldb'

copy 'ECELLDB.sql' into the SQL tab of myPHPadmin

Copy all the files to htdocs of XAMPP

in browser goto localhost

webpage should open

If you are a member of ECell, register your password first and then login.